package org.example;

import org.junit.runner.RunWith;
import org.junit.runners.Suite;

@RunWith(Suite.class)
@Suite.SuiteClasses({SnakeTest.class, GameplayTest.class})
public class Suite_Class_1 {
    // This class doesn't contain any code, it simply acts as a container for the test classes
}
